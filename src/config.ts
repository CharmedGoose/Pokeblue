export const GIF_FRAME_SKIP = 3;
export const GIF_DELAY = (5 * GIF_FRAME_SKIP).toString();

export const MIN_SPAWN_TIME = 300;
export const MIN_TIME_BETWEEN_SPAWNS = 60;

export const DEFAULT_MIN_SPAWN_TIME = 300;
export const DEFAULT_MAX_SPAWN_TIME = 360;
